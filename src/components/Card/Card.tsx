import React, { Component } from "react";
import { CardRoot } from "./Card.styles";

const Card = () => {
  return <CardRoot>
      <h2>test</h2>
  </CardRoot>;
};

Card.id = "Card";

export default Card;
