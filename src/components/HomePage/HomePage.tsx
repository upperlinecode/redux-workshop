import React, { Component } from "react";
import { HomePageRoot } from "./HomePage.styles";

const HomePage = (props: { content: string }) => {
  return <HomePageRoot></HomePageRoot>;
};

HomePage.id = "HomePage";

export default HomePage;
